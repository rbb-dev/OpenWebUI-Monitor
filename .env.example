# OpenWebUI 配置
OPENWEBUI_DOMAIN=your_openwebui_domain
OPENWEBUI_API_KEY=your_api_key
# INIT_BALANCE=1 # 初始余额，可选

# 默认模型价格配置（美元/百万tokens）
DEFAULT_MODEL_INPUT_PRICE=0.01  # 默认输入价格
DEFAULT_MODEL_OUTPUT_PRICE=0.03  # 默认输出价格

# 访问控制
ACCESS_TOKEN=your-access-token-here # 用于页面访问验证
API_KEY=your-api-key-here # 用于 API 请求验证

# PostgreSQL 数据库配置（如果使用外部数据库，请配置以下环境变量）
# POSTGRES_HOST=
# POSTGRES_PORT=
# POSTGRES_USER=
# POSTGRES_PASSWORD=
# POSTGRES_DATABASE=